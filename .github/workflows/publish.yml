name: Publish Pipeline

on:
  push:
    branches:
      - main

jobs:
  publish:
    runs-on: self-hosted

    steps:
      # 1) CHECKOUT
      - name: Checkout Code
        uses: actions/checkout@v3

      # 2) FETCH TAGS
      - name: Fetch all tags
        run: git fetch --tags

      # 3) AUTO SEMVER LOGIC
      - name: Determine Semantic Version
        id: semver
        run: |
          LAST_TAG=$(git describe --tags --abbrev=0 2>/dev/null || echo "v0.0.0")
          MAJOR=$(echo "$LAST_TAG" | cut -d. -f1 | sed 's/v//')
          MINOR=$(echo "$LAST_TAG" | cut -d. -f2)
          PATCH=$(echo "$LAST_TAG" | cut -d. -f3)
          NEW_PATCH=$((PATCH + 1))
          NEW_TAG="v${MAJOR}.${MINOR}.${NEW_PATCH}"
          echo "NEW_TAG=$NEW_TAG" >> $GITHUB_ENV
          echo "new_tag=$NEW_TAG"

      # 3.5) PUSH NEW TAG (MOST IMPORTANT)
      - name: Push Git Tag
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git tag ${{ env.NEW_TAG }}
          git push origin ${{ env.NEW_TAG }}

      # 4) LOGIN TO DOCKER HUB
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # 5) BACKEND IMAGE BUILD
      - name: Build Backend Docker Image
        working-directory: application-code/app-tier
        run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/backend:${{ env.NEW_TAG }} .

      - name: Push Backend Docker Image
        run: |
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/backend:${{ env.NEW_TAG }}
          docker tag ${{ secrets.DOCKERHUB_USERNAME }}/backend:${{ env.NEW_TAG }} ${{ secrets.DOCKERHUB_USERNAME }}/backend:latest
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/backend:latest

      # 6) FRONTEND IMAGE BUILD
      - name: Build Frontend Docker Image
        working-directory: application-code/web-tier
        run: docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/frontend:${{ env.NEW_TAG }} .

      - name: Push Frontend Docker Image
        run: |
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/frontend:${{ env.NEW_TAG }}
          docker tag ${{ secrets.DOCKERHUB_USERNAME }}/frontend:${{ env.NEW_TAG }} ${{ secrets.DOCKERHUB_USERNAME }}/frontend:latest
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/frontend:latest
      - name: Prevent duplicate tag
        run: |
          if git rev-parse "$NEW_TAG" >/dev/null 2>&1; then
            echo "Tag $NEW_TAG already exists. Exiting."
            exit 0
          fi

      # 7) CREATE RELEASE
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ env.NEW_TAG }}
          name: Release ${{ env.NEW_TAG }}
          body: |
            Automated release with Docker images:
            - backend:${{ env.NEW_TAG }}
            - frontend:${{ env.NEW_TAG }}
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PERSONAL_TOKEN }}


